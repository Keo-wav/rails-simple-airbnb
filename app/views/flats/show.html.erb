<h2><%= @flat.name %></h2>
<h4><em><%= @flat.address %></em></h4>
<p>About this flat : <%= @flat.description %></p>
<p>Price per night : <%= @flat.price_per_night %> EUR</p>
<p>Number of guests : <%= @flat.number_of_guests %></p>

<%= link_to 'Back to flats list', flats_path %>
<%= link_to 'Edit this flat', edit_flat_path %>
<%= link_to 'Delete this flat', flat_path(@flat), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
